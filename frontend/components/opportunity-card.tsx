import Link from "next/link";
import type { OpportunitySummary } from "@/types/opportunity";
export function OpportunityCard({ opportunity }: { opportunity: OpportunitySummary }) { const arrow = opportunity.momentum === "Surging" ? "↑↑" : opportunity.momentum === "Rising" ? "↑" : "→"; return <article className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-medium text-blue-700">{opportunity.category} · {opportunity.region}</p><h2 className="mt-2 text-lg font-semibold">{opportunity.title}</h2></div><div className="text-right"><p className="text-2xl font-semibold">{opportunity.marketScore}</p><p className="text-xs text-slate-500">Market score</p></div></div><p className="mt-3 text-sm text-slate-600">{opportunity.oneLineSummary}</p><div className="mt-4 rounded-lg bg-slate-50 p-3 text-sm"><span className="font-medium">Why now: </span>{opportunity.whyNow}</div><div className="mt-4 flex items-center justify-between"><span className="text-sm text-slate-500">{arrow} {opportunity.momentum} · Confidence {opportunity.confidenceScore}</span><Link className="rounded-md bg-slate-900 px-3 py-2 text-sm text-white" href={`/opportunities/${opportunity.id}`}>View evidence</Link></div></article>; }
